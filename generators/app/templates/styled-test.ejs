import { shallow } from 'enzyme';
import React from 'react';

import { <%= componentName %> } from './<%= path %>';

describe('<%= componentName %>', () => {
    it('should render <%= description %>', () => {
        expect(shallow(<<%= componentName %> />).render()).toMatchInlineSnapshot();
    });
});
